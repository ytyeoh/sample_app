<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="navbar-header navbar-primary">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Money", root_path, class: "navbar-brand" %>
      <%= form_tag listings_path, method: :get do %>

            <div class="input-group">
                <%= button_tag(type: "submit", class: "hide-") do %>
                  <i class="material-icons md-light">search</i>
                <% end %>       
              <%= text_field_tag :query, params[:query], class: 'form-control city', placeholder: "Where you want to rent?",  autocomplete: "off" %>
            </div>

      <% end %>
    </div>
    <div id="navbar" class="collapse navbar-collapse light-grey-background">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <% unless current_user.image.url.empty? %>
          <li><%= image_tag current_user.image.url(:thumb), class: "img-circle" %><span class="dark-green-text big-text">  <%= current_user.name %></span></li>
            <% end %>
          <li><a href="#" data-toggle="modal" data-target="#topup">Payment</a></li>
          <li><%= link_to 'Edit account', edit_user_path(current_user) %></li>
          <li><%= link_to current_user.name, root_path %></li>
        <% else %>
          <li><%= link_to 'Sign in', new_user_session_path %></li>
          <li><%= link_to 'Sign up', new_user_registration_path %></li>
        <% end %>
        <li><a href="#" data-toggle="modal" data-target="#topup">Payment</a></li>
        <li><%= link_to 'listings', listings_path,'data-no-turbolink' => true %></li>
        <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' if user_signed_in? %></li>
      </ul>
    </div>
  </nav>