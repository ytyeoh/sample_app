<%= form_for(@listing, html: { class: "form-horizontal", role: "form"， remote: true }) do |f| %>
  <%= f.hidden_field :published_at , :value => Time.now, class: "form-control" %>
  <%= f.submit "Bump",  class: "btn btn-primary" if @listing.published_at < 1.day.ago%>
<% end %>